apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/part-of: gpu-batch-inference-scheduler

resources:
  - configmap.yaml
  - service.yaml
  - deployment.yaml
  - job.yaml
