apiVersion: v1
kind: Service
metadata:
  name: gpu-scheduler
  labels:
    app: gpu-scheduler
spec:
  selector:
    app: gpu-scheduler
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
