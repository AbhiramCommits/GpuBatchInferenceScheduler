# Profiling the batched GEMM

This document summarizes Nsight Systems (nsys) and Nsight Compute (ncu)
profiling of the batched GEMM kernels, and records the transfer-vs-compute
analysis workflow.

> [!NOTE]
> **Measurements pending a GPU run.** The repository was finalized on a
> machine without a CUDA GPU, so no numbers are claimed here — the tables
> hold placeholders. Run `scripts/profile.sh` on a CUDA host (e.g. an A100
> or a GeForce-class GPU) and fill in the `—` cells.

## How to profile

```sh
scripts/profile.sh
# or with custom parameters:
M=512 N=512 K=512 BATCH=16 ITERS=50 IMPL=tiled scripts/profile.sh
```

which produces `results/nsys_report.sqlite`, `results/nsys_breakdown.md`,
and `results/ncu_tiled.ncu-rep`.

## Transfer vs compute breakdown (nsys)

Workload: `bench_gemm --m 1024 --n 1024 --k 1024 --batch 8 --iters 10
--impl tiled` (float32; `--batch 8` means 3 GiB of A+B+C traffic per run).

| Category | Time (ms) | % of wall time |
|---|---|---|
| GPU kernels | — | — |
| Memcpy HtoD | — | — |
| Memcpy DtoH | — | — |
| Other (CPU / gaps) | — | — |
| **Total** | — | 100.0 |

Expected to read directly from `results/nsys_breakdown.md` (generated by
`scripts/parse_nsys.py`).

## What pinned memory + stream overlap change

The scheduler and benchmark can be run in three configurations (pageable
host memory + synchronous copies; pinned `cudaHostAlloc` +
`cudaMemcpyAsync` on one stream; pinned + separate copy/compute streams
chained with events, as implemented in `BatchScheduler::execute_batch`):

| Configuration | H2D+D2H (ms) | Kernel (ms) | Total (ms) | Speedup |
|---|---|---|---|---|
| Pageable host, single stream, sync | — | — | — | 1.00x |
| Pinned + async, single stream | — | — | — | — |
| Pinned + copy stream overlapped with compute | — | — | — | — |

What we expect to observe, and what the table should confirm:

- **Pageable → pinned:** the driver can DMA straight from locked pages
  instead of staging through a bounce buffer, removing a per-copy
  `cudaMemcpy` synchronization point; copies typically drop by 3-6x.
- **Stream overlap:** with H2D on the copy stream and the kernel on the
  compute stream, iteration *i+1*'s copies hide under iteration *i*'s
  kernel, so the copy time visible on the critical path shrinks toward the
  transfer of a single iteration.
- The remaining "Other" wall time is CPU-side work (host allocation, RNG
  fill of inputs, event bookkeeping), which the scheduler amortizes in
  steady-state across many jobs.

## Kernel-level analysis (ncu)

`ncu --set full` on the tiled kernel (32x32x32 tiles, 1024 threads/block):

| Counter | Value |
|---|---|
| Achieved occupancy | — % (of 64 warps/SM max) |
| Occupancy limiter | — (registers / shared memory / blocks) |
| DRAM throughput | — % of peak |
| Shared memory throughput | — % of peak |
| Top warp stall reason | — |

## Identified bottleneck (expected)

**Synchronous shared-memory tile loads with no pipelining.** Each k0
iteration loads its A/B tiles into shared memory behind a full
`__syncthreads` before any FMA can run, so the SM stalls on global-load
latency every tile. Expect ncu's long-scoreboard stall counter to dominate
and shared-memory throughput to be far below peak. Fill in the measured
evidence from the ncu table above.

## Next optimization

**cp.async double buffering** (`cuda::memcpy_async`/libcudacxx or inline
`cp.async.cg.shared.global`):

1. Split the k-loop into a prologue plus a main loop with two shared-memory
   tile buffers per operand.
2. Issue the tile *k0+1* copy before computing tile *k0*, synchronized by
   `cp.async.commit_group`/`wait_group` instead of a per-tile
   `__syncthreads`.
3. Swizzle the shared-memory layout to stay bank-conflict-free.

Goal: hide global-load latency behind FMAs and push the kernel toward its
DRAM-bandwidth bound. After that, closing the remaining gap to cuBLAS
requires 128x128 tiles with warp-level outer products (CUTLASS-style SGEMM)
or TF32 tensor cores.
